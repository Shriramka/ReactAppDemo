var React = require('react');
var expect = require('expect');

var Rewards = require('Rewards');

describe('Rewards Component', () => {
  it('Rewards Component should exist', () => {
    expect(Rewards).toExist();
  });

  describe('Rewards Component Functionalities', () => {
     it('Test 1', () => {
      expect(1).toBe(1);
     });

     it('Test 2', () => {
      expect(1).toBe(1);
     });
  });
});
